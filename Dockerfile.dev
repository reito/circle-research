# 開発環境用Dockerfile
FROM node:20-alpine

RUN apk add --no-cache libc6-compat
WORKDIR /app

# 開発環境では package.json だけをコピーして依存関係をインストール
COPY package.json package-lock.json* ./
RUN npm ci

# ソースコードはボリュームマウントするので、ここではコピーしない

EXPOSE 3000

ENV PORT 3000
ENV HOSTNAME "0.0.0.0"

CMD ["npm", "run", "dev"]